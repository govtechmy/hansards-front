import axios from "axios";
const API_URL = process.env.API_URL;
const PATH = `api/file/download`;

/**
 * Deprecated legacy download implementation.
 * This file is intentionally left blank to prevent duplicate logic with `lib/downloads.ts`.
 * Remove after confirming all references migrated.
 */
export {};
